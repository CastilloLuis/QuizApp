import React, { Component } from 'react';
import { Button } from 'react-native-elements';

const StartButton = () => {
    return (
        <Button>START</Button>
    )
};

export default StartButton;